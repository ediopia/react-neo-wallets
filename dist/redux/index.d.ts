import { ReactNode } from "react";
import configureStore from "./store";
interface IProps {
    children: ReactNode;
}
export { configureStore };
export declare const ReduxProvider: (props: IProps) => JSX.Element;
//# sourceMappingURL=index.d.ts.map