import { LocalWallet } from "./local-wallet";
import { Invoke } from "./invoke";
import { Providers } from "./providers";

const FTW = {
  wallet: LocalWallet,
  invoke: Invoke,
  providers: Providers
};

export default FTW
